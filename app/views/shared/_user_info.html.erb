<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Ver mi cuenta", current_user %>
</span>
<span>
  <%= pluralize(current_user.challenges.count, "reto") %>
</span>
<%= link_to "Sign out", signout_path, method: "delete" %>